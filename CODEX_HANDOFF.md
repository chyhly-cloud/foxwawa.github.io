# CODEX_HANDOFF — foxwawa / jibunkatte

更新时间：2026-07-28（JST）  
交接范围：`/Users/hly/Documents/GitHub/foxwawa/jibunkatte`

## 项目目标

维护《自分勝手 / jibunkatte》作品集页面。页面的现有视觉系统和整体叙事结构应保留；后续工作只做已提出的细节修正，不重新设计整页。

当前重点是让页面中的研究资料、拼贴、图示与原作品集一致：

- 使用原 Illustrator/PDF 作为视觉与文字依据，保持图片原始色彩；
- 图片抠图要干净、保留主体信息，不能只把整张原图直接贴上；
- 图片、说明文字和对应关系正确；
- 桌面与手机上均可读，用户可见文字不小于 `14px`；
- 保持现有语言切换（中／日／英）与页面交互。

## 当前架构

这是一个没有构建步骤的静态多页面作品集仓库。`jibunkatte` 页面采用单个 HTML 文件承载样式、结构、文案和交互：

- 主页面：`jibunkatte/index.html`
- 样式：同一文件内的 `<style>`，以 CSS 变量和绝对定位的 `.beat-scene` 完成桌面编排；`@media (max-width:999px)` 提供移动端流式布局。
- 文案与国际化：`const COPY`（约第 1044 行）存储 `ja`、`zh`、`en`；`applyLanguage()`（约第 1211 行）更新 `data-i18n` 节点并把语言写入 `localStorage` 的 `hl-lang`。
- 交互：内联脚本包括研究思维图文字处理 `wrapMindmap()`、图片说明定位 `placeCaptions()`、灵感来源折叠、图片灯箱、展览/过程滚动与导航效果。
- 外部依赖仅包括 Google Fonts 和第 09 节的 YouTube iframe；无 npm、Vite、Webpack、测试框架或构建配置。

页面章节（`index.html`）：

1. Hero
2. `#question` — 概念
3. `#research` — 项目背景、思维图、社会／生活／个体研究
4. `#derivation` — 洞察到造型
5. `#plan-a`、`#experiment`、`#plan-b`
6. `#fabrication`、`#exhibition`、`#film`

## 已完成功能（当前代码已有）

- 完整的九段作品叙事、固定导航、语言循环、灵感折叠、图片灯箱和记录影像嵌入。
- 中／日／英文案字典与 `data-i18n` 绑定已经存在。
- 研究区域已使用“项目背景文献 + 思维图 + 社会／生活／个体”结构；已有大量从作品集提取并放入 `jibunkatte/` 的研究、拼贴、实验素材。
- 背景文献区最近已作细节调整：`.background-study` 增高到 `clamp(980px,126vh,1460px)`；`doc-1.jpg`、`doc-3.jpg` 设为 28% 宽，`doc-2.jpg` 设为 27.5% 宽并放到更低、更松的位置，以回应“所有书籍图片更大，规训与身体控制移下来”的要求。
- 已有移动端断点和动态说明位置处理，但下面的已知问题说明这些仍需人工视觉验收。

## 正在进行的工作

1. 用原作品集核准研究区的每一张图、图题与图注，尤其是社会／生活／个体三组研究图。
2. 校准已有的资产处理：用户认为多个 JPG/WebP 的颜色与原稿有差异，并要求抠图更加干净。
3. 完成项目背景文献排布的视觉 QA；最近刚改动，尚未在可用浏览器里做人工确认。
4. 修正移动端研究图、思维图、图片说明与横向可滚动提示的可读性。

## 未完成事项与已知问题

### 视觉/资产准确性

- 用户多次反馈图片颜色失真。当前已有大量被重新编码的已跟踪 JPG，不能继续批量调色或压缩；必须先和原稿逐张对照。
- 用户要求“干净抠图”，不是把带白底的原图直接放进页面。现有 `cut-*`、`pol-*`、`mk-*` 等 WebP 是待核对的抠图/拼贴资产，透明边缘、白边、主体完整性都需逐张检查。
- `interaction-diagram`/社会与个人关系图曾被用户评价为粗糙、与作品集差别大；在替换前必须先查看 PDF/AI 的原始视觉，不可凭文字重画一个新的概念图。
- 人体拼贴需与作品集的组合一致；穿泳裤的人脸要做遮挡；规训痕迹的每张图片要拆开、分散放置并在图片下配对应说明。

规训痕迹应对应的文字（由用户明确提供）：

1. 弹钢琴时不可以立起指甲。
2. 长时间书写会引起颈椎异常。
3. 厨师的手上常常留下烫伤的痕迹。
4. 手臂的长度在躯干长度中占据很大比例。
5. 游泳选手的身体会趋近三角形的结构。

### 文案与图文对应

- 研究区多张图仍缺少与原作品集逐图对应的说明；不要改变现有页面的大结构，只补回正确说明。
- 路线/地图图题应为“航路上の生活の変遷”，此前曾被写错。
- 用户给出的“生活”段落（日文）应用于对应上方内容：

  > 人と人との関係は、社会システムや環境の変化に伴い絶えず進化します。権力は、複雑な相互作用の中で生成され、社会ネットワーク内の位置や資源の分配に依存し、社会規範や制度の影響を受けます。環境の変化に応じて、権力の分布や運用も適応し、その表現形態は多様化します。

- 社会／个人／微生物图下方应使用用户给出的日文，而不是把它误归到上面的图：

  > 社会における規律は至る所に存在し、個人は規律の影響を受けつつ、社会全体にも影響を与えます。この点で、生物と人間の社会性の隠喩に魅了され、微生物や細胞と人体の関係を用いた表現手法を使いました。

- 思维图和研究图的日／英版本与可读性仍需检查，不能只把中文改好。

### 响应式和可读性

- 用户明确要求所有用户可见小字最小 `14px`，但仍要保留标题、正文、注释之间的层级差。
- `wrapMindmap()` 在狭窄视口曾造成中文/日文纵向断裂、元素重叠；移动端要优先使用稳定的单列或单独 SVG 布局，不能依赖脆弱的运行时重排。
- `placeCaptions()` 会在运行时重写图注的 `--t`，可能和绝对定位图片冲突；修改相关研究区域后须复测桌面和手机。
- 横向可滚动内容必须有清晰的滚动提示，不可假设用户会横滑。
- 用户反馈过去的移动端图片曾相互叠压、文字在虚实图线上挤成一团；任何局部调整必须检查 375px 左右宽度。

## 重要技术决策

- **不改整体设计。** 用户已明确：当前 Claude 设计方向要保留，后续只能做图像、文案、排布、可读性等细节调整。
- **以原稿为准。** 使用 `1.ai` 和 `Portfolio.pdf` 进行视觉核对；先截图/渲染比对，再改 HTML 或资产。
- **色彩优先级高于文件体积。** 不要为了压缩而重编码原始 JPG；当前色差投诉与此前资产处理有关。
- **移动端另行编排。** 桌面绝对定位艺术编排不能直接缩放到手机；对复杂区域使用专门的移动规则，而不是不断缩小所有元素。
- **不要清理 `tmp/`。** 其中含 PDF 渲染、色彩备份和检查资料，虽未追踪但对恢复与对照重要。
- **不提交、不部署。** 本次交接前没有创建提交、推送或发布。

## 已经失败的尝试（避免重复）

- 曾对图片进行重编码/色彩处理，用户认为“所有图片颜色都不对”。不要再批量处理图片；必须从原稿逐张验证。
- 曾尝试用 CSS/JS 自动重排复杂 SVG/图注来适配小屏。实际截图显示窄屏会出现垂直断字、重叠和错位；应优先使用清晰、稳定的移动端专用布局。
- 曾把研究和关系图按自己的理解重新绘制，用户认为和作品集不一致、过于粗糙。后续不得脱离原稿重画。
- 曾出现大范围改版倾向，用户明确否定：不要改变现有网页设计，只处理目标细节。
- 应用内浏览器此前拒绝直接访问 `file://` 页面，无法用该工具进行本地视觉回归；不要绕过限制。请改用本地 HTTP 静态服务器并由用户/可用浏览器验证。

## 修改过的关键文件与素材

### 代码

- `jibunkatte/index.html` — 当前开发主文件；包含全部 CSS、HTML、COPY 字典与 JS。相对 HEAD 有大量未提交变化。

### 当前未追踪的页面素材（按用途）

- 资料/背景：`doc-1.jpg`、`doc-2.jpg`、`doc-3.jpg`、`mindmap.webp`。
- 网络/地图/图示：`net-*.jpg`、`dia-*.webp`、`map-routes.jpg`、`gps-1.jpg` 至 `gps-9.jpg`、`sign-1.jpg` 至 `sign-8.jpg`。
- 人体/拼贴/抠图：`body-stage*.webp`、`cut-*.webp`、`pol-*.jpg`、`mk-*.webp`、`fig-*.webp`。
- 模型/实验：`mat-*.jpg`、`mod-*.jpg`、`sim-*.jpg`、`plana-*.jpg`、`ref-*.jpg`、`figure-*.{jpg,webp}`。

### 原始/参考材料（不在 Git 页面目录）

- Illustrator 原稿：`/Volumes/A/作品视频/hly/1.ai`
- 作品集 PDF：`/Users/hly/Library/Containers/com.tencent.xinWeChat/Data/Documents/xwechat_files/hyxyyxxhly_a8e3/msg/file/2025-01/Portfolio.pdf`
- PDF 渲染参考：`tmp/pdfs/jibunkatte-portfolio/`、`tmp/pdfs/jibunkatte-source/`
- 色彩回退资料：`tmp/color-backups/jibunkatte-20260727/` 与 `tmp/color-tests/`

## 启动、构建与测试命令

仓库没有发现 `package.json`、构建配置或自动测试。使用静态服务器即可：

```bash
cd /Users/hly/Documents/GitHub/foxwawa
python3 -m http.server 8899
```

浏览地址：`http://localhost:8899/jibunkatte/index.html`

可执行的静态检查：

```bash
cd /Users/hly/Documents/GitHub/foxwawa
git diff --check
git diff --check -- jibunkatte/index.html
```

需要同时人工检查桌面和手机宽度；没有现成的自动化视觉测试。

## 当前 Git 分支、状态与未提交改动

检查时间：2026-07-28（写入本交接文件前）。

- 当前分支：`main`
- 上游：`origin/main`（状态头为 `## main...origin/main`，未显示领先/落后）
- HEAD：`8065758` — `2026-07-22T14:50:59+09:00 Update index.html`
- 已修改的已跟踪文件：33 个；未追踪文件/目录：70 个（写入本文件后会额外出现 `CODEX_HANDOFF.md`，即未追踪项变为 71 个）。
- 尚未提交、推送或部署。

已跟踪修改包括：

- `.DS_Store`
- `jibunkatte/index.html`
- `jibunkatte/hero.jpg`、`jibunkatte/sim-3d.jpg`
- `jibunkatte/process-1.jpg` 至 `jibunkatte/process-13.jpg`
- `jibunkatte/exhibition-1.jpg` 至 `jibunkatte/exhibition-16.jpg`

当前 `git diff --stat`（写入本文件前）为 **33 files changed, 1343 insertions(+), 372 deletions(-)**。图片二进制文件多数显示尺寸变化；这正是后续应优先核对色彩的原因。

完整未追踪清单可由下列命令获得（不要因“未追踪”而直接删除 `tmp/` 或素材）：

```bash
git status --short
```

## 新线程建议的继续顺序

1. 在仓库根目录阅读此文件，并再次运行 `git status --short`，确认不覆盖现有未提交工作。
2. 先视觉检查 `Portfolio.pdf` 与 `1.ai`，定位每一张研究图、关系图、人体拼贴及其正确文案；建立“原稿页/素材文件/页面位置/说明文字”的对照表。
3. 从色彩备份和原稿选定可信原图。仅在确认需要时生成干净透明抠图，避免全局色彩或压缩操作。
4. 逐个完成用户已指出的微调：背景文献验证、图片说明映射、路线图标题、日文段落归属、人体拼贴/规训痕迹、关系图准确性。
5. 每做一组局部修改，使用 `python3 -m http.server 8899` 在桌面与约 375px 宽度人工检查；重点看语言切换、文字最小 14px、图片不重叠、图注与图片对齐和横滑提示。
6. 运行 `git diff --check`，再向用户汇报具体修正内容和仍需确认的视觉点。除非用户明确要求，不要提交、推送、部署或重做整体页面设计。
